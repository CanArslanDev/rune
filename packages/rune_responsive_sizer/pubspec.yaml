name: rune_responsive_sizer
description: Responsive-sizer bridge for the rune package. Registers .w / .h / .sp / .dm property extensions for percent-of-screen sizing.
version: 0.0.1
repository: https://github.com/CanArslanDev/rune/tree/main/packages/rune_responsive_sizer
homepage: https://github.com/CanArslanDev/rune
issue_tracker: https://github.com/CanArslanDev/rune/issues
topics:
  - rune
  - bridge
  - responsive
  - sizer
  - media-query

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  rune:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
